# Intentionally empty. vitest.config.ts points DOTENV_CONFIG_PATH here so that
# `import "dotenv/config"` in server modules loads NOTHING during tests — the
# suite must never inherit the developer's real .env (live keys, real prices).
# All env needed by a test is pinned in vitest.config.ts or stubbed per-test.
